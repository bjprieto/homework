"""
HW 2 Review

Constructing cross/interaction-matrices

for i in range(i):
	for j in range(x):
		...
		
x = 0 for full matrix
x = i for half matrix with major diagonal
x = i+1 for half matrix without major diagonal
"""

"""
"list" ~ "array" for the sake of this class, since .py arrays are unfavorable
and uncommonly used

lists ~ tuples, except mutable

take note of whether methods mutate data or not,
	e.g. L.sort() mutates the list into sorted list
"""